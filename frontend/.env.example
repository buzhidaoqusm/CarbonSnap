# Copy to .env or .env.local (do not commit secrets)
# When true, the app uses in-memory mock responses (no Flask required)
# VITE_USE_MOCK=true

# Optional API base URL; leave empty in dev to use the Vite /api proxy
# VITE_API_BASE_URL=http://127.0.0.1:5000
